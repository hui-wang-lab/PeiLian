# PeiLian ROADMAP

> ## 🚩 当前游标：**P4 — Persona 工厂 + CustomerState**
>
> 切换游标必须由**用户显式确认**。Claude Code 不得自动推进到下一阶段。
>
> **历史**：
> - P0 ✅ 已完成（commit `f7ba079`，2026-05-08）
> - P1 ✅ 已完成（commit `77bb98e`，2026-05-08）
> - P2 ✅ 已完成（commit `71fefc4`，2026-05-08）
> - P3 ✅ 已完成（commit `9cf2916`，2026-05-09）

---

## 阶段总览

| # | 阶段 | 一句话目标 | Demo 命令 | 验收一句话 |
|---|---|---|---|---|
| P0 ✅ | 工作流地基 | 项目骨架 + 文档体系 + 最小 LLM 往返 | `python scripts/demo_p0.py [--skip-llm]` | demo 跑通且文档体系建立 |
| P1 ✅ | 单 persona 单场景 | 1 个写死客户 + 1 个固定场景的 CLI 完整对话 | `python scripts/demo_p1.py` | 代理人能与该客户走完一轮对话且客户严格被动反应 |
| P2 ✅ | 状态观察器 + 规则评估 | 对话结束后输出必问点覆盖率 + 合规红线扫描 | `python scripts/demo_p2.py` | 给定一段对话能正确标出漏问与违规 |
| P3 ✅ | LLM-as-Judge 评估 | 加入话术质量、共情度、逻辑结构的模型评分 | `python scripts/demo_p3.py` | 同段对话多次评分稳定性 ±1 分 |
| P4 | Persona 工厂 + CustomerState | 配置化生成多种客户画像 + 运行时客户状态 | `python scripts/demo_p4.py` | 一份 yaml 能生成 5+ 种 persona，且客户信息披露/隐藏关切按状态逐步推进 |
| P5 | 产品 RAG | 接入条款/合规知识库，AI 客户问条款不胡编 | `python scripts/demo_p5.py` | 故意问条款细节，回答能溯源到知识库 |
| P6 | Web UI + 报告 | 浏览器里完成陪练并看到可视化报告 | `npm run dev` 或 `python -m peilian.server` | 端到端跑通，雷达图/逐句标注可见 |
| P7 | 错题本 + 自适应难度 | 多次训练后系统能推荐弱项专项练习 | `python scripts/demo_p7.py` | 连续 3 次对某场景扣分，下次自动加大该场景比重 |

---

## 各阶段详情

### P0 — 项目初始化与开发工作流地基  ✅ 已完成

**目标**：搭好可持续开发的项目骨架与上下文文档体系，证明地基跑得通。

**纵切范围**：
- Python 包结构 + 配置加载
- CLAUDE.md（项目宪法）+ ROADMAP.md（本文件）+ phase-0.md（阶段 spec）
- 最小 demo（配置加载 + 包导入 + LLM 单轮往返；支持 `--skip-llm`）

**验收摘要**：
- `pip install -e .` 成功
- `python scripts/demo_p0.py --skip-llm` 跑通地基验证
- `python scripts/demo_p0.py` 在配 key 后能拿到真实响应
- 文档体系完整且 CLAUDE.md 不含实现细节

详见 [`docs/phases/phase-0.md`](phases/phase-0.md)。

---

### P1 — 单 persona 单场景  ✅ 已完成

**目标**：把第一条业务逻辑跑通——1 个写死的客户画像 + 1 个固定场景，CLI 里能完整对话。

**纵切范围**：
- Persona 数据结构（最小可用，不追求完备）
- 对话引擎：多轮 LLM 调用 + 上下文维护
- System prompt 构造（强制"被动反应"原则）

**验收摘要**：
- 代理人能与「35 岁价格敏感型客户」完成一轮 KYC→产品讲解→异议→促成
- 客户严格遵守"被动反应"原则（人工对话观察验证）
- 代码中能看到"被动反应"的明确约束（system prompt + 测试用例）
- 引入 pytest，至少覆盖 persona 加载与 system prompt 渲染

**新引入**：persona schema、对话引擎、pytest 框架。

---

### P2 — 状态观察器 + 规则评估  ✅ 已完成

**目标**：对话结束后，输出基于规则的评估报告。

**纵切范围**：
- 状态观察器（识别销售漏斗阶段，纯观察不调度）
- 必问点覆盖率检查
- 合规红线词库扫描

**验收摘要**：
- 给定一段假对话，能正确标出漏问的必问点
- 能识别违禁话术（保证收益等）
- 状态观察器**不**影响对话生成（架构隔离）

---

### P3 — LLM-as-Judge 评估  ✅ 已完成

**目标**：加入模型层评估，覆盖规则层无法判断的维度。

**纵切范围**：
- LLM judge prompt + rubric
- 多次打分的稳定性测试
- 诊断 AI 客户是否过早暴露家庭/收入/已有保障/hidden_concerns，或出现前后矛盾

**验收摘要**：
- 同一段对话多次评分稳定性 ±1 分以内
- rubric 覆盖话术专业度、共情度、逻辑结构、异议处理质量
- 能指出 AI 客户自身的越界泄露或一致性问题，但不在 P3 改写对话生成链路

---

### P4 — Persona 工厂 + CustomerState  🚩 当前

**目标**：从配置生成多种 persona，可调难度档，并引入本轮对话内的 `CustomerState`，让 AI 客户按状态逐步披露信息与隐藏关切。

**纵切范围**：
- Persona yaml schema（隐藏关切、性格参数、坚持度等）
- Persona 工厂函数
- 难度档分级机制
- CustomerState v1：记录已披露字段、当轮允许回答字段、隐藏关切状态、信任度与耐心值
- 生成前状态摘要注入：把 CustomerState 转成面向 LLM 的简短约束，提升被动反应稳定性
- 生成后状态更新：根据代理人提问与客户回复更新已披露字段、隐藏关切触发/表达进度
- 保持状态观察与场景控制边界：CustomerState 只约束“可说/不可说/说到什么程度”，不按剧本强行推进话题

**验收摘要**：
- 一份 yaml 能生成 5+ 种行为差异可观测的 persona
- 难度档切换会显著改变客户行为（坚持度、信息隐藏度等）
- 同一轮陪练中，客户不会重复遗忘已披露信息；hidden_concerns 能按未触发→已触发→暗示→表达的节奏逐步释放
- 面对泛泛提问时客户保持模糊或反问；面对明确点名问题时只回答被问到的那一项

---

### P5 — 产品 RAG

**目标**：接入产品条款知识库，AI 客户被问条款细节时能溯源回答。

**纵切范围**：
- 知识库格式（条款/费率/健康告知/犹豫期）
- 检索器（先做最简单的关键词或 BM25，向量化按需）
- AI 客户在条款相关问题上必须走 RAG，不允许 LLM 自由发挥

**验收摘要**：
- 故意问条款细节，回答能溯源到知识库段落
- 知识库以外的产品条款问题，AI 客户会回避或说"我不太清楚"

---

### P6 — Web UI + 报告

**目标**：浏览器里完成一次陪练并看到可视化报告。

**纵切范围**：
- 后端 API（具体框架待定，候选 FastAPI）
- 前端（具体框架待定，候选 Next.js 或 Streamlit）
- 报告页：雷达图、逐句标注、合规扫描结果

**验收摘要**：
- 浏览器端到端跑通一次陪练
- 报告页含五维雷达图与逐句标注

---

### P7 — 错题本 + 自适应难度

**目标**：累积训练数据后能推荐弱项专项练习。

**纵切范围**：
- 错题本数据结构与持久化
- 弱项画像计算
- 自适应难度调度策略

**验收摘要**：
- 连续 3 次对「价格异议」扣分，下次训练该场景比重显著上升
- 用户能查看自己的弱项雷达图与历史趋势

---

## 不变量（所有阶段都遵守）

- 代理人驱动 + AI 客户被动反应（→ [CLAUDE.md §2.1](../CLAUDE.md)）
- 状态观察器 ≠ 场景控制器（→ [CLAUDE.md §2.2](../CLAUDE.md)）
- 合规红线（→ [CLAUDE.md §3](../CLAUDE.md)）
- 每阶段必须能用一行命令 demo
- 阶段切换由用户显式确认
